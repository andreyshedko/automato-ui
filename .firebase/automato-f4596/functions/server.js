import { onRequest } from 'firebase-functions/v2/https';
  const server = import('firebase-frameworks');
  export const ssrautomatof4596 = onRequest({}, (req, res) => server.then(it => it.handle(req, res)));
  